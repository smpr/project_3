import React, { Component } from 'react';

class ArticleListIndividual extends Component {
    render() {
        return (
            <div>
                {props.name}
            </div>
        );
    }
}

export default ArticleListIndividual;